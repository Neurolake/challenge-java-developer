spring:
  application:
    name: challenge-java-developer

  datasource:
    url: jdbc:mysql://localhost:3306/neurotech-client
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 5000